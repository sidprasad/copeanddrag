constraints:
    - orientation: {field: Next, directions: [directlyRight]}
    - orientation: {field: left, directions : [left]}
    - group : {field: refs}
    - group : {field: obj}
    - orientation: {field: refs, directions: [below]}
    - group : {field: done, target: domain}

directives:
    - flag: hideDisconnected
    - projection: {sig: so/Ord}
    - attribute : {field: keys}